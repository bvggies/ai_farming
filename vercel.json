{
  "buildCommand": "cd client && npm install && npm run build",
  "outputDirectory": "client/build",
  "rewrites": [
    {
      "source": "/api/posts/:id",
      "destination": "/api/posts/postById?id=:id"
    },
    {
      "source": "/(.*)",
      "destination": "/$1"
    }
  ]
}
